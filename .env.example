PORT=3000

# Required only when running the API server (artifacts/api-server):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Frontend (mockup-sandbox) — Laravel backend integration
# Without trailing slash. API calls go to ${BASE}/api/v1, Echo auth to ${BASE}/broadcasting/auth
VITE_API_BASE_URL=https://ivory-snail-183262.hostingersite.com

# Pusher credentials (ask the backend owner). Leave empty to skip realtime.
VITE_PUSHER_APP_KEY=
VITE_PUSHER_APP_CLUSTER=mt1
